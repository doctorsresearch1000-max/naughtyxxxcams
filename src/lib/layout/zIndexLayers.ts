/** Shared stacking scale (body-level fixed UI). */

/** `#nx-mobile-feed-stage` — set in globals.css */
export const Z_FEED_STAGE = 25;

/** `MobileBottomChrome` — CTA + bottom nav */
export const Z_BOTTOM_CHROME = 100_000;

/** Modal scrim / backdrop (portaled to body) */
export const Z_MODAL_SCRIM = 100_050;

/** Modal panel (portaled to body) */
export const Z_MODAL_PANEL = 100_060;
